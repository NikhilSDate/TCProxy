keyword = {"def-var"}
ip = @{ASCII_DIGIT{1, 3} ~ "." ~ ASCII_DIGIT{1, 3} ~ "." ~ ASCII_DIGIT{1, 3} ~ "." ~ ASCII_DIGIT{1, 3}}
ip_literal = @{"\"" ~ ip ~ "\""}
ident = @{!keyword ~ ASCII_ALPHA ~ (ASCII_ALPHANUMERIC | "-")*}
vardef = {"(" ~ "def-var" ~ " " ~ ident ~ " " ~ ip_literal ~ ")"}

