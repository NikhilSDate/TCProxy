name: Testing

on:
  workflow_dispatch:
  push:
  pull_request:
    types: [opened]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: test
      run: cargo test --verbose

